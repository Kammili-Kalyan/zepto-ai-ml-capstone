## Model Summary

The Analytics module performs exploratory data analysis, classification, and regression on the Titanic dataset.

Three classification models were evaluated:
- Logistic Regression
- Decision Tree
- Random Forest

A Linear Regression model was also developed to predict fare.

The complete preprocessing and Random Forest classification pipeline is saved as `model_pipeline.joblib` and can be reloaded for predictions on raw passenger data.